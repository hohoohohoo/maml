{
  "experiment_name": "gnn_maml_tsmc_sweep",
  "description": "TSMC GNN MAML architecture sweep (7D node features: base electrical features only)",
  "base_config": {
    "corner" : "FF",
    "temperature" : "0",
    "data_type": "cell",
    "graph_mode": "full_graph",
    "meta_lr": 0.0001,
    "innerdiv": 10,
    "total_iterations": 30000,
    "chunk_size": 10000,
    "K": 5,
    "tasks_per_meta_batch": 16,
    "inner_steps": 1,
    "gpu": 1,
    "pooling": "mean"
  },
  "sweep_params": {
    "conv_hidden_dim": [32],
    "num_conv_layers": [2],
    "fc_hidden_dim": [512],
    "num_fc_layers": [2]
  },
  "_comments": {
    "pooling_options": "mean, max, add, output (output-node-only)",
    "output_node_idx_note": "output_node_idx is dynamically determined from topology cache (fallback default: 2)"
  }
}
