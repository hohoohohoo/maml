{
  "experiment_name": "gnn_baseline_tsmc_process_sweep",
  "description": "TSMC Process GNN Baseline architecture sweep (11D node features: 7 base + 4 process params)",
  "base_config": {
    "data_type": "cell",
    "graph_mode": "stage_aware",
    "lr": 0.0001,
    "wd": 0.005,
    "batch_size": 5,
    "total_iterations": 300000,
    "chunk_size": 100000,
    "gpu": 3
  },
  "sweep_params": {
    "conv_hidden_dim": [64],
    "num_conv_layers": [2],
    "fc_hidden_dim": [256],
    "num_fc_layers": [2]
  },
  "_comments": {
    "training_type": "Standard mini-batch training (NOT MAML)",
    "optimizer": "Adam with weight decay",
    "batch_size_note": "Number of samples per mini-batch from randomly selected task",
    "lr_note": "Learning rate for Adam optimizer",
    "wd_note": "Weight decay (L2 regularization)"
  }
}
